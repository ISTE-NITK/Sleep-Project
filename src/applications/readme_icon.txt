Create desktop icon with provided .desktop file:

Add icon image to /usr/share/pixmaps

Add application script to /home/pi/<desired_directory> and 
change exec parameter accordingly.

Keep terminal = true to ensure application runs in foreground.

